import React from "react";

const WelcomePage = props => {
    return (
        <main>
            <h1>Welcome to the Amazon Application React</h1>
        </main>
    );
}

export default WelcomePage;